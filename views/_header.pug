header.header
    nav.nav.nav__log
    .header__logo
        img(src='images/logo-white.png' alt='logo')
    nav.nav.nav--user
        if user
            a.nav.nav__el.nav__el--logout Logout
            a.nav.nav__el(href='/') 
                //- img.nav__user-img(src='img/users/default.jpg' alt='user img')
                span= user.name.split(' ')[0]
        else
            a.nav__el(href='#') Sign Up
            a.nav__el.nav__el--cta(href='/login') Login